npm install for node_modules

npm install nodemailer for nodemailer

.env with the information:

MY_EMAIL= 
MY_PW =  
HOSTNAME = is the hostname or IP address to connect to (defaults to ‘localhost’)
PORT = defaults to 587 if is secure is false or 465 if true
WEB = 
